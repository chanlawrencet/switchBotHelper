# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfvars
*.auto.tfvars

# Lambda build artifacts / vendored deps
lambda.zip
lambda/.venv/
lambda/__pycache__/
lambda/**/*.pyc
lambda/**/*.pyo
lambda/**/*.pyd
lambda/**/__pycache__/
lambda/**/*.so

# Vendored Python packages inside lambda/
lambda/bin/
lambda/include/
lambda/lib/
lambda/share/
lambda/*.dist-info/
lambda/certifi/
lambda/charset_normalizer/
lambda/idna/
lambda/requests/
lambda/urllib3/

# macOS
.DS_Store

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version

# Local helper environment
.env
